---
name: PR Checks

# pull_request, not pull_request_target: no job here needs a write token or a
# secret on a fork PR, so there is nothing to gain from running privileged
# against untrusted code — which is what pull_request_target does. This also
# matches azure-pipelines-terraform and -packer.
#
# That sentence was untrue for twelve lines: `dependency-review` held
# `pull-requests: write` while the comment claimed no job needed one (#34). The
# grant is now `read`, so the file and its own rationale agree — which is the
# point, because a comment that reads as authoritative is what stops the next
# reader checking.
"on":
  pull_request:
    types: [opened, edited, synchronize, reopened]

permissions:
  contents: read

jobs:
  title:
    name: PR title convention
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      pull-requests: read
    steps:
      # hardening-exception: egress-audit — this job holds no stored credential
      # (its GITHUB_TOKEN is read-only and scoped to this PR), and its egress is
      # a third-party action's own dependency fetches rather than a set this
      # file can enumerate. The two jobs in this repository that DO hold a
      # stored App private key — release-please and signature-replay's replay —
      # are on `block` with an explicit allowlist (#23).
      - uses: step-security/harden-runner@05e31511f85b41b11d1cf0ef85d0992719546e2c # v2.21.0
        with:
          egress-policy: audit
      - uses: amannn/action-semantic-pull-request@48f256284bd46cdaab1048c3721360e808335d50 # v6.1.1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          types: |
            feat
            fix
            chore
            docs
            test
            refactor
            ci
            deps
            security
            perf
            revert
          requireScope: false

  dependency-review:
    name: Dependency review
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
      # read, not write. The pinned action documents the write grant as
      # existing for ONE input — `comment-summary-in-pr` — and that input is
      # not set here, so the grant was dead while the header comment eight
      # lines above claimed "neither job needs a write token" (#34). This job
      # has an untrusted-by-construction tree on disk; the file and its own
      # justification now agree.
      pull-requests: read
    steps:
      # hardening-exception: egress-audit — this job holds no stored credential
      # (its GITHUB_TOKEN is read-only and scoped to this PR), and its egress is
      # a third-party action's own dependency fetches rather than a set this
      # file can enumerate. The two jobs in this repository that DO hold a
      # stored App private key — release-please and signature-replay's replay —
      # are on `block` with an explicit allowlist (#23).
      - uses: step-security/harden-runner@05e31511f85b41b11d1cf0ef85d0992719546e2c # v2.21.0
        with:
          egress-policy: audit
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/dependency-review-action@a1d282b36b6f3519aa1f3fc636f609c47dddb294 # v5.0.0
        with:
          fail-on-severity: high

  # ── Breaking-change declarations survive the squash ────────────────────
  # This repository squash-merges with
  # `squash_merge_commit_message=COMMIT_MESSAGES` (verified on the live
  # repository, not assumed), so every commit body in a pull request is
  # concatenated into ONE merge commit. release-please then keeps only the
  # FIRST `BREAKING CHANGE:` footer of that commit, and reads a `!` marker only
  # from its header. A second declaration anywhere in the PR is dropped in
  # silence: the change ships with no changelog entry, no upgrade note, and
  # nothing failing to say so.
  #
  # WHAT COUNTS is release-please's rule, not the spec's, and the two spellings
  # of the token are NOT symmetric there. Verified against release-please
  # 17.11.1's own parser rather than its documentation: `BREAKING CHANGE:`, with
  # a space, is read only at the START of a line, while the hyphenated spelling
  # is ALSO matched by an unanchored regex over the commit body -- mid-sentence,
  # indented, inside a fenced code block, even inside a longer word -- with the
  # remainder of that line taken as the description.
  #
  # This guard used to count only line-anchored matches, so it UNDER-counted
  # against release-please and passed commits release-please treats as breaking.
  # abacdb5 in azure-pipelines-terraform, the commit that ADDED the guard there,
  # is the case: a sentence in its body NAMING the hyphenated spelling made
  # release-please propose a MAJOR bump over 1.14.4 with a changelog entry
  # reading "` spelling". The guard counted it 0.
  #
  # So the count below is release-please's count -- no more, and no less, since a
  # guard that rejects legitimate prose is one people route around and then
  # delete. On top of it the guard rejects a hyphenated token found off the start
  # of a line at all: that is never a declaration anybody WROTE, and it always
  # buys a major version bump nobody asked for.
  #
  # This is not hypothetical, and it is not this repository's own near-miss.
  # terraform-registry-backend v4.0.0 published with two undocumented breaking
  # changes this way -- one of them 403s any CI job calling `/auth/refresh` with
  # an API key -- and terraform-state-manager-backend lost three from a PR that
  # declared four, caught only by reading the notes line by line before the cut.
  #
  # Splitting the footers across separate commits does not help; the squash
  # concatenates them back. One merged commit can announce exactly one breaking
  # change. Either open one PR per breaking change, or combine them into a
  # single footer and write each up in the upgrade guide.
  #
  # PROVENANCE. Ported from `terraform-registry-backend`'s
  # .github/workflows/pr-checks.yml, which carries this job together with
  # terraform-registry-frontend, terraform-state-manager-{backend,frontend} and
  # terraform-suite-identity. NOT ported from the two sibling extensions: the
  # estate note says all seven repositories carry this guard, and they do not --
  # `azure-pipelines-terraform` and `azure-pipelines-packer` have neither the
  # job nor any equivalent (`gh search code --repo … "BREAKING"` finds only
  # their commit-message-check prose). Both squash with COMMIT_MESSAGES like
  # everything else here, so the gap is real and belongs on their backlogs; this
  # port is deliberately the five-repo variant rather than a third one.
  #
  # MUTATION-PROVED, but no longer here. The guard was a 128-line shell body
  # duplicated across NINE repositories -- which is why one regex mistake existed
  # in nine places at once and took nine pull requests to correct -- so it now
  # lives once as a composite action in 4cloudguru/shared-workflows, with its
  # 16-case test beside it in that repository's self-check.
  #
  # The test still EXTRACTS the script from the action rather than copying it: a
  # copy drifts from the thing it claims to prove, which is the same defect one
  # level up.
  #
  # This stays a composite action called as a STEP rather than a reusable
  # workflow, so the job keeps its name and this check keeps reporting as
  # "Breaking-change footers survive the squash". A reusable workflow reports as
  # <caller-job-id> / <called-job-name>, and this context is required in
  # azure-pipelines-release-docs.
  #
  # A NEW required context has to be added to main's branch protection by hand:
  # "Breaking-change footers survive the squash". Until that is done this job
  # reports on every pull request without blocking one — which is also the
  # state of "release-please can read the merged commit" below, the other half
  # of the release-parsing pair, and both should be promoted together.
  release-pr-minor-bumps:
    name: Release PR Minor Bumps
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      # hardening-exception: egress-audit — this job clones this repository and
      # holds no stored credential; its remaining egress is the runner's own
      # action fetches rather than a set this file can enumerate (#23).
      - uses: step-security/harden-runner@05e31511f85b41b11d1cf0ef85d0992719546e2c # v2.21.0
        with:
          egress-policy: audit
      # Always runs, so it can be a required check: a gate that only exists on
      # release PRs cannot be required, and one that is not required does not
      # block the merge it exists to block.
      - name: Skip for non-release PRs
        if: ${{ ! startsWith(github.event.pull_request.head.ref, 'release-please--') }}
        env:
          HEAD_REF: ${{ github.event.pull_request.head.ref }}
        run: |
          echo "Head ref '${HEAD_REF}' is not a release-please branch; the per-task Minor-bump gate only applies to Release PRs. Skipping."
      # Tags are the comparison basis, so fetch-depth: 0 and fetch-tags. The
      # merge ref this checks out is what main becomes when the PR merges, which
      # is the state the published task versions have to be correct for.
      - name: Checkout (release PRs only)
        if: ${{ startsWith(github.event.pull_request.head.ref, 'release-please--') }}
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          fetch-tags: true
          persist-credentials: false
      - name: Verify per-task Minor bumps
        if: ${{ startsWith(github.event.pull_request.head.ref, 'release-please--') }}
        run: node scripts/check-minor-bumps.js

  breaking-change-footers:
    name: Breaking-change footers survive the squash
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
      pull-requests: read
    steps:
      # hardening-exception: egress-audit — this job holds no stored credential
      # (its GITHUB_TOKEN is read-only and scoped to this PR) and reaches only
      # the GitHub API through `gh`; its remaining egress is the runner's own
      # action fetches rather than a set this file can enumerate. The two jobs
      # in this repository that DO hold a stored App private key —
      # release-please and signature-replay's replay — are on `block` with an
      # explicit allowlist (#23).
      - uses: step-security/harden-runner@05e31511f85b41b11d1cf0ef85d0992719546e2c # v2.21.0
        with:
          egress-policy: audit
      # The guard is 128 lines of shell that was duplicated across NINE
      # repositories, which is why one regex mistake existed in nine places at
      # once and took nine pull requests to correct. It now lives once, in
      # 4cloudguru/shared-workflows, with its test beside it.
      #
      # A composite ACTION, not a reusable workflow: a reusable workflow reports
      # as <caller-job-id> / <called-job-name>, which would rename this check.
      # Called as a step, the job keeps its name and the context keeps reporting
      # as "Breaking-change footers survive the squash", so no branch protection
      # moves.
      - name: Count breaking-change declarations across this PR's commits
        uses: 4cloudguru/shared-workflows/.github/actions/breaking-change-footers@68cee21133ae19352ee69570a7747838a442b659 # v1.16.0
        with:
          gh-token: ${{ secrets.GITHUB_TOKEN }}
          pr-number: ${{ github.event.pull_request.number }}
          repo: ${{ github.repository }}
  release-please-can-read-the-commit:
    name: release-please can read the merged commit
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
      pull-requests: read
    steps:
      # hardening-exception: egress-audit — this job holds no stored credential
      # (its GITHUB_TOKEN is read-only and scoped to this PR), and its egress is
      # a third-party action's own dependency fetches rather than a set this
      # file can enumerate. The two jobs in this repository that DO hold a
      # stored App private key — release-please and signature-replay's replay —
      # are on `block` with an explicit allowlist (#23).
      - uses: step-security/harden-runner@05e31511f85b41b11d1cf0ef85d0992719546e2c # v2.21.0
        with:
          egress-policy: audit

      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      # --ignore-scripts, matching every other install in this repository: the
      # parser is pure JS and needs no lifecycle script to run, and this job
      # has the PR's tree on disk (#21).
      - name: Install the parser release-please uses
        working-directory: .github/commit-message-check
        run: npm ci --ignore-scripts

      - name: Parse the commit this PR would squash into main
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          PR_TITLE: ${{ github.event.pull_request.title }}
          REPO: ${{ github.repository }}
        run: |
          set -euo pipefail

          gh api --paginate "repos/$REPO/pulls/$PR_NUMBER/commits" \
            --jq '.[] | {sha: .sha, message: .commit.message, parents: (.parents | length)}' \
            > commits.ndjson

          node .github/commit-message-check/verify.mjs commits.ndjson
